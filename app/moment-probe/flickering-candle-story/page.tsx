/** THROWAWAY PROBE — intro variation, the story version. Delete with the folder. */
import CandleStoryDemo from '../CandleStoryDemo';

export default function FlickeringCandleStoryPage() {
  return <CandleStoryDemo />;
}
