/** THROWAWAY PROBE — story intro with the candle OMNI made from CLAUDE'S prompt. */
import CandleStoryDemo from '../CandleStoryDemo';

export default function FlickeringCandleClaudeOmniPage() {
  return <CandleStoryDemo video="/candle-claude-omni.mp4" />;
}
