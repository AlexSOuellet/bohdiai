/** THROWAWAY PROBE — story intro with the candle Seedance made from CLAUDE'S prompt
 * (the "Bohdi writes the prompt" test). Delete with the folder. */
import CandleStoryDemo from '../CandleStoryDemo';

export default function FlickeringCandleClaudePage() {
  return <CandleStoryDemo video="/candle-claude-prompt.mp4" />;
}
