/** THROWAWAY PROBE — story intro with the candle KLING made from CLAUDE'S prompt. */
import CandleStoryDemo from '../CandleStoryDemo';

export default function FlickeringCandleClaudeKlingPage() {
  return <CandleStoryDemo video="/candle-claude-kling.mp4" />;
}
